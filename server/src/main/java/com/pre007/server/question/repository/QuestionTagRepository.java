/*
package com.pre007.server.question.repository;

import com.pre007.server.question.entity.Question;
import com.pre007.server.question.entity.QuestionTag;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.repository.query.Param;

import java.util.List;

public interface QuestionTagRepository extends JpaRepository<QuestionTag, Long> {

    List<QuestionTag> findAllQuestionId(@Param("questionId") long questionId);

    List<QuestionTag> findAllQuestionAndQuestionTagStatus(Question question, QuestionTag.QuestionsTagStatus questionsTagStatus);
}
*/
